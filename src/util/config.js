module.exports = {
  db: {
    database: process.env.BT_DB_NAME,
    host: process.env.BT_DB_HOST,
    port: process.env.BT_DB_PORT,
    user: process.env.BT_DB_USER,
    password: process.env.BT_DB_PASSWORD
  }
}
